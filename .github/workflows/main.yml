name: Todo API CI/CD

on:
  push:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Build Docker image
      run: docker build -t todo-api .
      
    - name: Run container
      run: docker run -d -p 3000:3000 --name todo-app todo-api
      
    - name: Wait for app to start
      run: sleep 5
      
    - name: Test API endpoints
      run: |
        # Test root endpoint
        curl -f http://localhost:3000/
        
        # Test get all todos
        curl -f http://localhost:3000/todos
        
        # Test create todo
        curl -f -X POST http://localhost:3000/todos \
          -H "Content-Type: application/json" \
          -d '{"task": "Test from CI/CD"}'
      
    - name: Stop container
      run: docker stop todo-app
